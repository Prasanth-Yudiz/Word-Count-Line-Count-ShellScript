# Word-Count-Line-Count-ShellScript

This is the shell script for printing the word number and the line number of a given word in the file. If the word has more than one occurence print all. 

Pull the code from the github.

To run the shell script

make it executable
chmod +x code.sh

To execute it or run it.
./code.sh
